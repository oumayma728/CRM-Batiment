{
  "name": "crm-batiment",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.12.0"
  },
  "overrides": {
    "glob": "^11.0.0"
  },
  "scripts": {
    "install:apps": "npm --prefix backend install && npm --prefix frontend install",
    "start": "npm run dev",
    "dev": "concurrently -n backend,frontend \"npm --prefix backend run start:dev\" \"wait-on http-get://localhost:3000/api && npm --prefix frontend run dev\"",
    "dev:backend": "npm --prefix backend run start:dev",
    "dev:frontend": "npm --prefix frontend run dev"
  },
  "devDependencies": {
    "concurrently": "^9.2.1",
    "wait-on": "^9.0.10"
  }
}
